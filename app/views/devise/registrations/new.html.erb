<div class="container">
  <div class="row">
    <!-- 左のブロック -->
    <div class="col-md-6">
      <div class="display-container">
        <h1 style="margin-bottom: 20px"> My Tools へようこそ！</h1>
        <ul>
          <li>1.会員登録をします</li>
          <li>2.簡単なプロフィールを登録します</li>
          <li>3.自分が使用しているアプリのスクリーンショットを投稿します</li>
          <li>4.好きなアプリとレビューを投稿します</li>
          <li>5.他の人がどんなアプリを使っているか見てみましょう！</li>
        </ul>

        <!-- サンプル画像(モーダルできる) -->
        <a data-toggle="modal" data-target=".bs-example-modal-lg">
          <%= image_tag('top_image.png', class: "img-responsive") %>
        </a>

        <!-- モーダル -->
        <div class="modal fade bs-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel">
          <div class="modal-dialog modal-lg">
            <div class="modal-content">
              <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h2 class="modal-title" id="myModalLabel">サンプル画像</h2>
              </div>
              <div class="modal-body">
                <%= image_tag('top_image.png', class: "img-responsive") %>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-dismiss="modal" style="width: 100px">閉じる</button>
              </div>
            </div>
          </div>
        </div>

      </div>
    </div>

    <!-- 右のブロック -->
    <div class="col-md-6">
      <div class="display-container">
        <h2>Registration</h2><br>
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <div class="form-box">
          <%= f.label :username %><br>
          <span>※ログイン時に使用できます</span>
          <%= f.text_field :username, placeholder: 'ユーザーネーム', autofocus: true, placeholder: "(半角英数字・記号(_)・4~20文字)", pattern: "[a-zA-Z0-9_]{4,20}", required: true %>
          <%= f.label 'e-mail' %>
          <%= f.email_field :email, placeholder: 'メールアドレス', required: true %>
          <%= f.label 'Password' %>
          <%= f.password_field :password, placeholder: '6文字以上', autocomplete: "off", required: true %>
          <%= f.label 'Password (confirmation)' %>
          <%= f.password_field :password_confirmation, placeholder: "確認のためもう一度入力してください", autocomplete: "off", required: true %>
          <%= f.submit "会員登録する", class: "btn btn-info btn-lg btn-block active" %>
          <%= link_to 'ログインページに戻る', users_path, class: "btn btn-info btn-lg btn-block active" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
