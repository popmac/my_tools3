<div class="col-sm-4">
  <div>
    <h3>登録日:<%= photo.updated_at %></h3>
  </div>
  <div>
    <img src="<%= photo.image %>" style="width: 100%;">
  </div>
  <div>
    <%= link_to '削除する', "/users/#{current_user.id}/photos/#{photo.id}", method: :delete, data: {confirm: "本当に削除しますか？"}%>
  </div>
</div>
