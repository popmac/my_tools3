$("#sort-menu").text("ユーザー登録 (古い順)").append("<span></span>");
$("#sort-menu span").addClass("caret");
$("#user_list").html("<%= j(render partial: "users/user_list", locals: { users: @users }) %>");
$(".sort-dropdown-menu").hide();
$(".list-group-item" + ".active").removeClass("active");
